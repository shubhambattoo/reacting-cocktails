const initialState = {
  allCocktails: [],
  error: null,
  isLoading: false,
};

function cocktails(state = initialState, action) {
  switch (action.payload) {
    default:
      return state;
  }
}

export default cocktails;
